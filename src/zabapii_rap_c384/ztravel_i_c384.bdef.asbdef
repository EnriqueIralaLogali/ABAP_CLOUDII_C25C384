interface;
use draft;

define behavior for ZTRAVEL_I_C384 alias Travel
{
  use create;
  use update;
  use delete;

  use action acceptTravel;
  use action rejectTravel;
  use action deductDiscount;
  use action validateCustomerID;
  use action validateAgencyID;
  use action validateDates;

  use action Resume;
  use action Edit;
  use action Activate;
  use action Discard;
  use action Prepare;
}